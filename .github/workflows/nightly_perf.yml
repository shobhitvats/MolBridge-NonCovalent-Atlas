#############################################
# NIGHTLY PERFORMANCE WORKFLOW DISABLED
# Date: 2025-09-30
# Original backup at:
#   docs/workflow_backups/nightly_perf.yml.bak
# This placeholder only allows manual dispatch
# and contains an always-skipped noop job.
#############################################

name: Nightly Performance Trend (disabled)

on:
  workflow_dispatch:
    inputs:
      note:
        description: "Placeholder – restore backup to re-enable nightly performance tracking"
        required: false
        default: ""

jobs:
  noop:
    runs-on: ubuntu-latest
    if: ${{ false }}  # always skipped
    steps:
      - name: Disabled placeholder
        run: echo "Nightly workflow disabled. Restore backup to re-enable."
